
<!doctype html>
<html lang="en" class="h-100">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <meta name="generator" content="Hugo 0.88.1">
    <title>IOT Dashboard - Status</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="canonical" href="https://getbootstrap.com/docs/5.1/examples/cover/">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
     <link href="/css/app.css" rel="stylesheet">
     <link href="/css/status.css" rel="stylesheet">
  </head>
  <body class="d-flex h-100 text-center text-white bg-dark">
    
<div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
  <%- include('components/navbar', {active:"status"}); %>

  <main class="px-3">
    <% if(status=="error") { %>
        <div class="alert alert-danger" role="alert">
            Ops, Something went wrong 
        </div>
    <%}%>
    <h1>Device status</h1>
    <table class="table table-light top-table">
        <thead class="table-light">
          <tr class="table-light">
            <th class="table-light" scope="col">Name</th>
            <th class="table-light" scope="col">Type</th>
            <%if(device.on_off) { %>
                <th class="table-light" scope="col">On/Off</th>
             <%}%>
            <%if(device.open_locked) { %>
                <th class="table-light" scope="col">Open/Locked</th>
             <%}%>
             <%if(device.open_closed) { %>
                <th class="table-light" scope="col">Open/Closed</th>
             <%}%>
             <%if(device.volume) { %>
                <th class="table-light" scope="col">Volume</th>
             <%}%>
             <%if(device.temperature) { %>
                <th class="table-light" scope="col">Temperature</th>
             <%}%>
          </tr>
        </thead>
        <tbody class="table-light">
            <tr class="table-light">
                  <td class="table-light"><%= device.name %></td>
                  <td class="table-light"><%= device.type %></td>
                  <%if(device.on_off) { %>
                    <td class="table-light"><%= device.on_off %></td>
                  <%}%>
                  <%if(device.open_locked) { %>
                    <td class="table-light"><%= device.open_locked %></td>
                 <%}%>
                 <%if(device.open_closed) { %>
                    <td class="table-light"><%= device.open_closed %></td>
                 <%}%>
                 <%if(device.volume) { %>
                    <td class="table-light"><%= device.volume %></td>
                 <%}%>
                 <%if(device.temperature) { %>
                    <td class="table-light"><%= device.temperature %></td>
                 <%}%>
            </tr>
        </tbody>
      </table>
      <p class="lead">
        <a href="/status" class="btn btn-lg btn-secondary fw-bold border-white bg-white">View a different device</a>
      </p>
  </main>

  <%- include('components/footer'); %>
</div>


    
  </body>
</html>
